import { createTheme } from "@mui/material";

const todoTheme = createTheme({
  palette: {
    primary: {
      main: "#007fff",
    },
  },
  shape: {
    borderRadius: 0,
  },
});

export default todoTheme;
